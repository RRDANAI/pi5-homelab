# PostgreSQL Database Configuration
# Root user credentials for PostgreSQL (used for initial setup)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=n8n
# Non-root PostgreSQL user for n8n application
POSTGRES_NON_ROOT_USER=
POSTGRES_NON_ROOT_PASSWORD=
# Domain Configuration
# Your Tailscale domain for private access (e.g., your-hostname.your-tailnet.ts.net)
TS_DOMAIN=ts.net
# Your public domain name for webhooks (e.g., example.com)
DOMAIN_NAME=example.com
# n8n Encryption Key
# IMPORTANT: Generate a random encryption key using:
# openssl rand -hex 32
# Keep this key safe - losing it means you can't decrypt stored credentials!
ENCRYPTION_KEY=
